start: expr

expr: expr "+" term -> add
    | expr "-" term -> sub
    | term

term: term "*" factor -> mul
    | term "/" factor -> div
    | factor

factor: NUMBER -> num
        | "(" expr ")"
        | "-" factor -> neg

%import common.NUMBER
%import common.WS
%ignore WS